version: "3"

services:
  web:
    build:
      context: .
      args:
        - DEV=true
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
